#include<stdio.h>
main() 
{
//to print sum of digits of given number
Int n, i,sum=0;
Scanf("%sd", &n) ;
Sum=0:
while(n>0) 
{
r=n%10
sum=sum+r;
n=n/10;
}
Printf("\n%d", sum) ;
n=sum;
}
Printf("\n%d", sum) ;
}
